import { findCommand } from "../command-path.js";
import { addCommandExamples, addCommandNotes, commandExamples } from "./notes.js";
/** Help-only workflow guidance. Syntax and validation belong to the native commands. */
export function registerGuidance(root) {
    const examples = {
        "project show": [""],
        "project capabilities": [""],
        "project rename": ['"Lobby"'],
        "invitations create": ["--email operator@example.com", "--email operator@example.com --link", "--kind ad-buyer --email buyer@example.com --slot-id ads_LOBBY --policy review_required"],
        "invitations list": ["", "--kind member --status queued"],
        "invitations revoke": ["inv_INVITATION"],
        "dashboard reset-sign-in": ["--email operator@example.com"],
        "ads networks list": [""],
        "ads networks show": ["prj_SELLER"],
        "ads network show": [""],
        "ads network create": ['--name "Main Street Centre"'],
        "ads network rate": ["--rate-mcr-per-15s 150000 --expect-rev 3"],
        "ads inventory list": [""],
        "ads inventory update": ["scr_LOBBY --enabled --audience-tags families,recreation"],
        "ads slots list": [""],
        "ads slots create": ['--name "Lobby break" --accepted-media image,video'],
        "ads slots update": ["ads_LOBBY --max-video-duration-ms 60000 --expect-rev 2"],
        "ads memberships list": [""],
        "ads memberships update": ["mem_MEMBERSHIP --policy trusted --expect-rev 2"],
        "ads memberships revoke": ["mem_MEMBERSHIP"],
        "ads creatives list": [""],
        "ads creatives create": ['--media-id med_MEDIA --copy "Half-price passes this week"'],
        "ads creatives show": ["cre_CREATIVE"],
        "ads campaigns list": [""],
        "ads campaigns show": ["cmp_CAMPAIGN"],
        "ads campaigns create": ["campaign.json"],
        "ads campaigns update": ["cmp_CAMPAIGN campaign.json --expect-rev 2"],
        "ads campaigns preview": ["cmp_CAMPAIGN"],
        "ads campaigns activate": ["cmp_CAMPAIGN --quote-id quo_QUOTE --expect-rev 2"],
        "ads campaigns pause": ["cmp_CAMPAIGN --expect-rev 2"],
        "ads campaigns resume": ["cmp_CAMPAIGN --expect-rev 2"],
        "ads campaigns accept-rates": ["cmp_CAMPAIGN --quote-id quo_QUOTE --expect-rev 2"],
        "ads reviews list": [""],
        "ads reviews approve": ["rev_REVIEW"],
        "ads reviews reject": ['rev_REVIEW --reason "Logo is unreadable at ten seconds"'],
        "ads reports spend": ["--campaign-id cmp_CAMPAIGN"],
        "ads reports delivery": ["--from 2026-09-01T00:00:00Z --to 2026-09-08T00:00:00Z"],
        "ads reviews show": ["rev_REVIEW"],
        "billing balance": [""],
        "billing statement": ["--limit 20"],
        "agent enroll": [
            '--email operator@example.com --project-name "Lobby" --name Lobby-agent',
            "--force --email operator@example.com",
            "--intent advertising --email operator@example.com",
        ],
        "agent connect": ["--print-url", "--wait --timeout 10000"],
        "agent status": [""],
        "agent disconnect": ["--yes"],
        "dashboard": [""],
        "dashboard open": [""],
        "app pack": ["./lobby-app --output lobby-app.tar.gz"],
        "app upload": ['./lobby-app --name "Lobby app"'],
        "app update": ["app_APP ./lobby-app"],
        "app list": [""], "app show": ["app_APP"],
        "media upload": ["./poster.png --tag Lobby", "./lobby-loop.wav --tag Lobby"],
        "media upload-batch": ["uploads.json --state uploads-state.json"],
        "media show": ["med_MEDIA"], "media download": ["med_MEDIA --output poster.webp"],
        "media list": ["--tag Lobby --primitive image"],
        "media update": ["med_MEDIA --tag Lobby"],
        "media delete": ["med_MEDIA"],
        "compose catalog": [""], "compose render": ["slide.json --output rendered"],
        "compose batch": ["slides.json --output rendered"],
        "playlist validate": ["lobby.json"], "playlist preview": ["lobby.json --output preview --contact-sheet"],
        "playlist templates": [""], "playlist create": ["lobby.json"],
        "playlist update": ["pl_PLAYLIST lobby.json"],
        "playlist export": ["pl_PLAYLIST --output lobby-bundle"],
        "playlist import": ['lobby-bundle --name "Lobby copy"', "lobby-bundle --update pl_PLAYLIST"],
        "playlist list": [""], "playlist delete": ["pl_PLAYLIST"],
        "screen publish": ["scr_SCREEN lobby.json"],
        "screen pair": ['234567 --name "Lobby screen"'],
        "screen provision": ['--open --name "Lobby screen"'],
        "screen update": ['scr_SCREEN --name "Lobby screen"'],
        "screen list": ["", "--state archived"], "screen show": ["scr_SCREEN"],
        "screen assign": ["scr_SCREEN --playlist-id pl_PLAYLIST"],
        "screen set-timezone": ["scr_SCREEN --timezone America/Los_Angeles"],
        "screen archive": ["scr_SCREEN"], "screen unarchive": ["scr_SCREEN"],
        "screen reload": ["scr_SCREEN"],
        "screen delete": ["scr_SCREEN"], "screen rotate-public-id": ["scr_SCREEN"],
        "screen toast": ['scr_SCREEN --text "Welcome" --duration-ms 5000'],
        "screen screenshot": ["scr_SCREEN --output screenshot.webp"],
        "screen storage-forecast": ["scr_SCREEN --playlist-id pl_PLAYLIST", "scr_SCREEN --playlist-id pl_PLAYLIST --playlist-rev 7"],
        "browser setup": ["--code SETUP_CODE --open"],
        "kv get": ["greeting --app-id app_APP"],
        "kv set": ['greeting --app-id app_APP --json-value \'{"message":"Welcome"}\'', "greeting --app-id app_APP --file greeting.txt --content-type text/plain"],
        "kv list": ["--app-id app_APP"], "kv delete": ["greeting --app-id app_APP"],
        "comment show screen": ["scr_SCREEN"], "comment show playlist": ["pl_PLAYLIST --page page_1"],
        "comment set screen": ["scr_SCREEN --file comments.json"],
        "comment set playlist": ["pl_PLAYLIST --page page_1 --file comments.json"],
        "comment delete screen": ["scr_SCREEN"], "comment delete playlist": ["pl_PLAYLIST --page page_1"],
        "operations show": ["op_OPERATION"], "operations wait": ["op_OPERATION --timeout 120000"],
        "operations cancel": ["op_OPERATION"],
        "events list": ["--limit 20"], "events follow": ["--timeout 30000"],
        "playback list": ["--screen-id scr_SCREEN"],
        "feedback bug": ['"Preview failed" --body-file report.md'],
        "feedback feature": ['"New workflow" --body-file request.md'], "feedback list": ["--kind bug"],
        "doctor": [""], "version": [""], "recovery list": [""],
        "recovery show": ["RECOVERY_ID"], "recovery reconcile": ["RECOVERY_ID"],
    };
    for (const [path, suffixes] of Object.entries(examples)) {
        const command = findCommand(root, path.split(" "));
        if (!command)
            throw new Error(`Unknown example command ${path}`);
        if (!commandExamples(command).length)
            addCommandExamples(command, ...suffixes.map(suffix => `screenrig ${path}${suffix ? ` ${suffix}` : ""}`));
    }
    const notes = {
        "invitations create": "Invites people to this project or buyers to its advertising network. Email delivery does not prove receipt. Email invitations expire after seven days; member links expire after 24 hours. At most 50 invitations may be outstanding per project. --link prints one URL once instead of emailing; --email identifies the intended recipient for the operator, but a link is not email-bound. Deliver it privately and never store or log it. Retry unchanged input after an ambiguous failure to reuse its saved Idempotency-Key.",
        "dashboard reset-sign-in": "Unauthenticated: never enrolls, sends a stored token, or changes stored credentials. The acknowledgment is identical for known and unknown addresses. Instructions expire after one hour. An unresolved request retains only its retry key and fingerprint in the local write ledger.",
        "agent enroll": "Creates a new project and first agent, even when the contact email belongs to another project. Ask for the contact email and supply --email. --project-name sets its display name; --name names this installation. A member invitation is emailed to the contact address. An already enrolled installation reuses its project. --force discards pending enrollment or connection state; intentional existing-project reconnects use agent connect. --intent advertising enables advertiser=true/screens=false without changing billing-plan assignment. A pending enrollment keeps its original email, name, and purpose.",
        "project capabilities": "Read this project's plan id, independent advertiser/screens feature flags, feature revision, and the server's effective capability set. Never infer permission from a quota, plan name, or command inventory. A route denial still wins over previously read capabilities.",
        "dashboard": "Opens the dashboard origin without minting credentials. If the browser opener fails, the origin is printed. Sign in with a retained person credential.",
        "dashboard open": "Opens the dashboard origin without minting credentials. The origin is printed only if the browser opener fails.",
        "app pack": "Local and unauthenticated. Pack an already-built static directory with a root index.html. app upload packs the directory itself; packing first is optional. Inline <style> and <script> blocks move into files under _screenrig/inline/ because the release CSP blocks inline code; inline event handlers and javascript: URLs are refused with the file and line.",
        "app upload": "Creates a new application and its first immutable release. Packs the built directory and waits for processing by default (120000 ms, polling every 1000 ms). With --no-wait, use operations wait on the returned operation_id before using the release.",
        "app update": "Creates a new immutable release for the same application and preserves its K/V. Existing playlists remain pinned to their previous release. Use playlist replace-release to preview and apply an explicit replacement. Wait defaults: 120000 ms, polling every 1000 ms.",
        "media upload": "Transcodes by default and requires ffmpeg and ffprobe. Audio (WAV, AAC/M4A, OGG, FLAC) becomes a 192 kb/s MP3 for a playlist soundtrack; an MP3 source uploads unchanged. --no-transcode uploads accepted bytes unchanged. Waits for processing by default (120000 ms, polling every 1000 ms). For multiple files use media upload-batch with a manifest.",
        "media upload-batch": "Reuse the manifest to resume with automatic private state; --state optionally selects its path. Default concurrency is 4 (range 1–8). Transcodes by default; ffmpeg and ffprobe are required unless --no-transcode is used. Upload polling defaults to 1000 ms.",
        "playlist validate": "Local schema and semantic validation; no authentication required. --lint-only is accepted for compatibility: this command always validates locally. Authorization and readiness still require server checks.",
        "playlist create": "Creates a new remote playlist without assigning screens. Supply a canonical {name, pages} document, not an inspection envelope. Display names may repeat; IDs identify playlists. Use playlist init to prepare a document or screen publish to create and assign it.",
        "playlist update": "Replaces the authored document and affects every assigned screen. First use playlist show ID --output FILE; edit that document; optionally supply its revision with --expect-rev. Use --overwrite to replace an authoring file; parent directories are created automatically.",
        "playlist show": "Plain show is an inspection response. --editable returns data.document and revision; --output writes the editable document to a file (--overwrite replaces an existing file) and reports the revision on stdout. When both are supplied, --output takes precedence.",
        "playlist export": "The output directory must not exist. Exports referenced images and videos; application primitives cannot be exported and the export refuses by default. Pass --skip-applications to drop application primitives and any page left with no primitives; the result reports the skipped primitive and page ids. Dynamic selectors are snapshotted to explicit media IDs.",
        "playlist import": "Creates a new playlist by default. Display names may repeat. Updating an existing playlist requires --update and affects all its assigned screens; --expect-rev is optional.",
        "screen publish": "Creates a new playlist and assigns it; does not update an existing playlist by name. Optionally guard against concurrent changes with --expect-rev. Inspect the prepared document and preview first. Repeat identical input to resume an ambiguous failure. After a revision conflict inspect the screen and follow the returned recovery instructions. Assignment readback does not prove playback; request and inspect screen screenshot separately.",
        "screen assign": "Assigns an existing playlist. --expect-rev optionally checks the screen revision. Scheduled playlists require the screen timezone. Use screen update to set timezone and assignment together.",
        "screen list": "--state archived also lists screens archived because their player was reset on the display (device_reset) or a paired browser unpaired itself (device_unpair); the REASON column shows archive_reason when the server reports it. screen unarchive restores any of them and re-admits the same display key. A row marked applications unsupported has a player that cannot show the application or web page primitives in its playlist.",
        "screen show": "An archived screen shows archive_reason and archived_at when the server reports them: project (an project or dashboard archive), device_reset (the player was reset on the display), or device_unpair (a paired browser unpaired itself). Every archive keeps the display's binding, so screen unarchive re-admits the same key and a display that still holds it resumes with no re-pairing. A display reset on the device may start pairing with a new key; that pairing can appear here as recovery_pending, and screen recover moves this screen to the new key (the screen stays archived until screen unarchive). applications_unsupported means the paired player cannot show application or iframe primitives; its manifest is unchanged and the player skips them.",
        "screen archive": "Archiving removes the screen from the default list, releases its screen quota, and darkens the display, but keeps the display's binding: the player stays connected, never re-pairs, and resumes on screen unarchive. Whoever holds the display's key can still connect while it is archived. To retire hardware for good, leave the screen archived or move the screen to new hardware with screen recover, which retires the old key.",
        "screen unarchive": "Restores a screen archived for any reason, including one archived because its player was reset on the display (device_reset) or a paired browser unpaired itself (device_unpair). It re-admits the same display key, so a display that still holds it resumes with no re-pairing; the screen must still fit the project's screen quota. A key retired by a confirmed screen recover stays retired.",
        "screen reload": "Sends the screen's player one reload request, valid for ten minutes, and returns its reload_id and expires_at. A web player reloads at its next page boundary; a native player reconnects, refetches its manifest, and checks for an update. A player ignores a reload within ten minutes of the last one it acted on. Works on active and archived screens; a screen still waiting to pair answers resource_conflict. The screen revision does not change. Repeat with the same --idempotency-key to get the original reload_id back instead of sending another.",
        "screen screenshot": "Requests, waits for, and downloads a screenshot. Default wait is 35000 ms and polling interval is 500 ms. Stdout reports the saved path, never image bytes. Inspect the downloaded image to assess playback.",
        "kv get": "Returns bytes as data.value_base64, including JSON values. Decode base64 and parse JSON only when data.content_type indicates JSON. kv list omits values.",
        "kv set": "--json-value encodes JSON directly. --file reads a local file or stdin with -; --value-base64 accepts canonical padded base64, including empty bytes. Byte modes default to application/octet-stream; --content-type overrides it. JSON mode also accepts an explicit application/json. Omit --expect-rev to create or overwrite; supply it to reject concurrent changes.",
        "comment set": "Replaces comments with a JSON value from --json-value or --file, including - for stdin. Last write wins; comments do not take --expect-rev or bump the resource revision.",
        "operations wait": "Wait defaults to 120000 ms, polling every 1000 ms. --timeout sets the wait budget. Use the operation_id returned by an accepted asynchronous command.",
        "events follow": "Writes NDJSON: one JSON envelope per event on stdout. --after resumes from a cursor; --cursor is a compatibility alias. --timeout is in milliseconds; zero or omitted leaves the stream unbounded.",
        "screen storage-forecast": "Read-only fit dry run: answers whether one playlist would fit the named screen's last reported storage before it is assigned, with the same target selection as the forecast in screen show (plan A.3 only, no transition prediction). It writes nothing: no assignment, no screen revision, no event. fit is unknown when the screen has never reported storage or the playlist's content references are not ready, and the byte counts and report time are null then. A report older than 24 hours is marked (stale) but is still forecast from. --playlist-rev refuses a playlist that changed since you read it with revision_conflict.",
    };
    for (const [path, note] of Object.entries(notes)) {
        const command = findCommand(root, path.split(" "));
        if (!command)
            throw new Error(`Unknown guidance command ${path}`);
        addCommandNotes(command, note);
        if (path === "comment set")
            for (const child of command.commands)
                addCommandNotes(child, note);
    }
}
//# sourceMappingURL=guidance.js.map