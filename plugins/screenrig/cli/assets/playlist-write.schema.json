{
  "$comment": "Generated by screenrig-protocol-gen from api/openapi.yaml; sha256=6983886a19203ca3401a69a98a1312ca485491755d8db272282677c10b3d3c79. Shape validation only; run canonical playlist-write-semantics.ts next. Reference resolution and DNS remain server checks.",
  "$defs": {
    "CanvasBackground": {
      "description": "Solid canonical uppercase #RRGGBBAA, or a top-to-bottom linear gradient.",
      "oneOf": [
        {
          "$ref": "#/$defs/CanvasColor"
        },
        {
          "$ref": "#/$defs/LinearGradientBackground"
        }
      ]
    },
    "CanvasColor": {
      "description": "Canonical uppercase sRGB RRGGBBAA.",
      "pattern": "^#[0-9A-F]{8}$",
      "type": "string"
    },
    "LinearGradientBackground": {
      "additionalProperties": false,
      "description": "Top-to-bottom linear gradient. There is no angle field. stops has 2 through 8 entries, strictly increasing at in [0, 1], first at=0, last at=1.",
      "properties": {
        "stops": {
          "items": {
            "$ref": "#/$defs/LinearGradientStop"
          },
          "maxItems": 8,
          "minItems": 2,
          "type": "array"
        },
        "type": {
          "enum": [
            "linear"
          ],
          "type": "string"
        }
      },
      "required": [
        "type",
        "stops"
      ],
      "type": "object"
    },
    "LinearGradientStop": {
      "additionalProperties": false,
      "description": "One stop on a top-to-bottom linear canvas background. at=0 is the top edge, at=1 is the bottom edge.",
      "properties": {
        "at": {
          "maximum": 1,
          "minimum": 0,
          "type": "number"
        },
        "color": {
          "$ref": "#/$defs/CanvasColor"
        }
      },
      "required": [
        "at",
        "color"
      ],
      "type": "object"
    },
    "PageVisibility": {
      "additionalProperties": false,
      "description": "Optional page visibility schedule. It is a sibling of advance and is never part of screenrig.canvas/v1; it describes playback orchestration, not geometry. The server validates and carries these rules as data. The player evaluates them against the screen timezone published at the manifest root. A page is eligible when enabled is true, now is inside [from, until), and either windows is absent or at least one window matches. Every playlist must keep at least one page with no visibility field at all, so eligible content always exists. Conformance vectors for the evaluation rules are the screenrig.schedule/v1 family in packages/schedule-contract.",
      "properties": {
        "enabled": {
          "description": "False hides the page unconditionally and needs no clock. A visibility object that sets enabled true and carries no from, until, or windows is a no-op and is rejected.",
          "type": "boolean"
        },
        "from": {
          "description": "Inclusive civil lower bound in the screen timezone, minute precision, no offset and no zone suffix.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}T([01]\\d|2[0-3]):[0-5]\\d$",
          "type": "string"
        },
        "until": {
          "description": "Exclusive civil upper bound in the screen timezone. When both bounds are present, from must be strictly earlier than until.",
          "pattern": "^\\d{4}-\\d{2}-\\d{2}T([01]\\d|2[0-3]):[0-5]\\d$",
          "type": "string"
        },
        "windows": {
          "items": {
            "$ref": "#/$defs/PageVisibilityWindow"
          },
          "maxItems": 16,
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "enabled"
      ],
      "type": "object"
    },
    "PageVisibilityWindow": {
      "additionalProperties": false,
      "description": "One recurring civil window. Omitting start and end selects the whole day. When end is less than or equal to start the window crosses midnight and the start day owns it, so fri 22:00 to 02:00 runs into Saturday morning.",
      "properties": {
        "days": {
          "description": "Civil days of week. The server stores them in mon-to-sun order; the set carries no author-visible ordering.",
          "items": {
            "enum": [
              "mon",
              "tue",
              "wed",
              "thu",
              "fri",
              "sat",
              "sun"
            ],
            "type": "string"
          },
          "maxItems": 7,
          "minItems": 1,
          "type": "array",
          "uniqueItems": true
        },
        "end": {
          "description": "Exclusive civil end time. Required when start is present.",
          "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
          "type": "string"
        },
        "start": {
          "description": "Inclusive civil start time. Required when end is present.",
          "pattern": "^([01]\\d|2[0-3]):[0-5]\\d$",
          "type": "string"
        }
      },
      "required": [
        "days"
      ],
      "type": "object"
    },
    "PlaylistAdvanceWrite": {
      "oneOf": [
        {
          "$ref": "#/$defs/PlaylistDurationAdvance"
        },
        {
          "$ref": "#/$defs/PlaylistApplicationAdvance"
        },
        {
          "$ref": "#/$defs/PlaylistMediaEndAdvanceWrite"
        }
      ]
    },
    "PlaylistApplicationAdvance": {
      "additionalProperties": false,
      "properties": {
        "max_ms": {
          "maximum": 86400000,
          "minimum": 1000,
          "type": "integer"
        },
        "mode": {
          "enum": [
            "application"
          ],
          "type": "string"
        }
      },
      "required": [
        "mode",
        "max_ms"
      ],
      "type": "object"
    },
    "PlaylistApplicationPrimitiveWrite": {
      "additionalProperties": false,
      "properties": {
        "application_id": {
          "description": "Optional ownership assertion; the server resolves the authoritative application from release_id.",
          "type": "string"
        },
        "content_fit": {
          "enum": [
            "fill"
          ],
          "type": "string"
        },
        "controller": {
          "default": false,
          "type": "boolean"
        },
        "enter": {
          "$ref": "#/$defs/PrimitiveEnter"
        },
        "id": {
          "pattern": "^[A-Za-z][A-Za-z0-9_-]{0,63}$",
          "type": "string"
        },
        "layer": {
          "maximum": 1024,
          "minimum": 0,
          "type": "integer"
        },
        "primitive": {
          "enum": [
            "application"
          ],
          "type": "string"
        },
        "rect": {
          "$ref": "#/$defs/PlaylistRect"
        },
        "release_id": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "primitive",
        "release_id",
        "rect",
        "layer",
        "content_fit"
      ],
      "type": "object"
    },
    "PlaylistCanvas": {
      "additionalProperties": false,
      "properties": {
        "background": {
          "$ref": "#/$defs/CanvasBackground"
        },
        "height": {
          "exclusiveMinimum": 0,
          "type": "number"
        },
        "viewport_fit": {
          "default": "contain",
          "enum": [
            "contain",
            "cover",
            "fill"
          ],
          "type": "string"
        },
        "width": {
          "exclusiveMinimum": 0,
          "type": "number"
        }
      },
      "required": [
        "width",
        "height",
        "background"
      ],
      "type": "object"
    },
    "PlaylistDurationAdvance": {
      "additionalProperties": false,
      "properties": {
        "after_ms": {
          "maximum": 86400000,
          "minimum": 1000,
          "type": "integer"
        },
        "mode": {
          "enum": [
            "duration"
          ],
          "type": "string"
        }
      },
      "required": [
        "mode",
        "after_ms"
      ],
      "type": "object"
    },
    "PlaylistIframePrimitiveWrite": {
      "additionalProperties": false,
      "properties": {
        "content_fit": {
          "enum": [
            "fill"
          ],
          "type": "string"
        },
        "enter": {
          "$ref": "#/$defs/PrimitiveEnter"
        },
        "id": {
          "pattern": "^[A-Za-z][A-Za-z0-9_-]{0,63}$",
          "type": "string"
        },
        "layer": {
          "maximum": 1024,
          "minimum": 0,
          "type": "integer"
        },
        "primitive": {
          "enum": [
            "iframe"
          ],
          "type": "string"
        },
        "rect": {
          "$ref": "#/$defs/PlaylistRect"
        },
        "src": {
          "format": "uri",
          "pattern": "^https://",
          "type": "string"
        },
        "title": {
          "maxLength": 200,
          "minLength": 1,
          "type": "string"
        }
      },
      "required": [
        "id",
        "primitive",
        "src",
        "title",
        "rect",
        "layer",
        "content_fit"
      ],
      "type": "object"
    },
    "PlaylistImagePrimitiveWrite": {
      "additionalProperties": false,
      "properties": {
        "alt": {
          "maxLength": 300,
          "type": "string"
        },
        "content_fit": {
          "enum": [
            "contain",
            "cover",
            "fill"
          ],
          "type": "string"
        },
        "dwell_ms": {
          "description": "Required on media_end image primitives. Forbidden on duration and application pages.",
          "maximum": 86400000,
          "minimum": 1000,
          "type": "integer"
        },
        "enter": {
          "$ref": "#/$defs/PrimitiveEnter"
        },
        "id": {
          "pattern": "^[A-Za-z][A-Za-z0-9_-]{0,63}$",
          "type": "string"
        },
        "layer": {
          "maximum": 1024,
          "minimum": 0,
          "type": "integer"
        },
        "primitive": {
          "enum": [
            "image"
          ],
          "type": "string"
        },
        "rect": {
          "$ref": "#/$defs/PlaylistRect"
        },
        "selector": {
          "$ref": "#/$defs/PlaylistMediaSelector"
        }
      },
      "required": [
        "id",
        "primitive",
        "selector",
        "rect",
        "layer",
        "content_fit"
      ],
      "type": "object"
    },
    "PlaylistMediaEndAdvanceWrite": {
      "additionalProperties": false,
      "properties": {
        "max_ms": {
          "description": "Optional client safety hint; the server resolves the authoritative deadline from resolved item lengths.",
          "maximum": 86400000,
          "minimum": 1000,
          "type": "integer"
        },
        "mode": {
          "enum": [
            "media_end"
          ],
          "type": "string"
        }
      },
      "required": [
        "mode"
      ],
      "type": "object"
    },
    "PlaylistMediaSelector": {
      "oneOf": [
        {
          "$ref": "#/$defs/PlaylistMediaSelectorByID"
        },
        {
          "$ref": "#/$defs/PlaylistMediaSelectorByIDs"
        },
        {
          "$ref": "#/$defs/PlaylistMediaSelectorByAll"
        },
        {
          "$ref": "#/$defs/PlaylistMediaSelectorByTag"
        }
      ]
    },
    "PlaylistMediaSelectorByAll": {
      "additionalProperties": false,
      "properties": {
        "by": {
          "enum": [
            "all"
          ],
          "type": "string"
        },
        "one_at_a_time": {
          "default": false,
          "type": "boolean"
        }
      },
      "required": [
        "by"
      ],
      "type": "object"
    },
    "PlaylistMediaSelectorByID": {
      "additionalProperties": false,
      "properties": {
        "by": {
          "enum": [
            "id"
          ],
          "type": "string"
        },
        "media_id": {
          "pattern": "^med_",
          "type": "string"
        },
        "one_at_a_time": {
          "default": false,
          "enum": [
            false
          ],
          "type": "boolean"
        }
      },
      "required": [
        "by",
        "media_id"
      ],
      "type": "object"
    },
    "PlaylistMediaSelectorByIDs": {
      "additionalProperties": false,
      "properties": {
        "by": {
          "enum": [
            "ids"
          ],
          "type": "string"
        },
        "media_ids": {
          "items": {
            "pattern": "^med_",
            "type": "string"
          },
          "maxItems": 32,
          "minItems": 1,
          "type": "array",
          "uniqueItems": true
        },
        "one_at_a_time": {
          "default": false,
          "type": "boolean"
        }
      },
      "required": [
        "by",
        "media_ids"
      ],
      "type": "object"
    },
    "PlaylistMediaSelectorByTag": {
      "additionalProperties": false,
      "properties": {
        "by": {
          "enum": [
            "tag"
          ],
          "type": "string"
        },
        "one_at_a_time": {
          "default": false,
          "type": "boolean"
        },
        "tag": {
          "pattern": "^[A-Za-z0-9]{1,32}$",
          "type": "string"
        }
      },
      "required": [
        "by",
        "tag"
      ],
      "type": "object"
    },
    "PlaylistPageWrite": {
      "additionalProperties": false,
      "properties": {
        "advance": {
          "$ref": "#/$defs/PlaylistAdvanceWrite"
        },
        "canvas": {
          "$ref": "#/$defs/PlaylistCanvas"
        },
        "id": {
          "pattern": "^[A-Za-z][A-Za-z0-9_-]{0,63}$",
          "type": "string"
        },
        "primitives": {
          "items": {
            "$ref": "#/$defs/PlaylistPrimitiveWrite"
          },
          "maxItems": 24,
          "minItems": 1,
          "type": "array"
        },
        "transition": {
          "$ref": "#/$defs/PlaylistTransition"
        },
        "visibility": {
          "$ref": "#/$defs/PageVisibility"
        }
      },
      "required": [
        "id",
        "canvas",
        "transition",
        "advance",
        "primitives"
      ],
      "type": "object"
    },
    "PlaylistPrimitiveWrite": {
      "oneOf": [
        {
          "$ref": "#/$defs/PlaylistApplicationPrimitiveWrite"
        },
        {
          "$ref": "#/$defs/PlaylistImagePrimitiveWrite"
        },
        {
          "$ref": "#/$defs/PlaylistVideoPrimitiveWrite"
        },
        {
          "$ref": "#/$defs/PlaylistIframePrimitiveWrite"
        }
      ]
    },
    "PlaylistRect": {
      "additionalProperties": false,
      "properties": {
        "height": {
          "exclusiveMinimum": 0,
          "type": "number"
        },
        "width": {
          "exclusiveMinimum": 0,
          "type": "number"
        },
        "x": {
          "type": "number"
        },
        "y": {
          "type": "number"
        }
      },
      "required": [
        "x",
        "y",
        "width",
        "height"
      ],
      "type": "object"
    },
    "PlaylistTransition": {
      "additionalProperties": false,
      "description": "Page transition. type and duration_ms are both required; OpenAPI does not default them. Swipe authoring examples use duration_ms 600; that value is not a schema default.",
      "properties": {
        "duration_ms": {
          "maximum": 60000,
          "minimum": 0,
          "type": "integer"
        },
        "type": {
          "enum": [
            "crossfade",
            "swipe-left",
            "swipe-right",
            "swipe-up",
            "swipe-down"
          ],
          "type": "string"
        }
      },
      "required": [
        "type",
        "duration_ms"
      ],
      "type": "object"
    },
    "PlaylistVideoPrimitiveWrite": {
      "additionalProperties": false,
      "properties": {
        "content_fit": {
          "enum": [
            "contain",
            "cover",
            "fill"
          ],
          "type": "string"
        },
        "enter": {
          "$ref": "#/$defs/PrimitiveEnter"
        },
        "id": {
          "pattern": "^[A-Za-z][A-Za-z0-9_-]{0,63}$",
          "type": "string"
        },
        "layer": {
          "maximum": 1024,
          "minimum": 0,
          "type": "integer"
        },
        "loop": {
          "default": false,
          "type": "boolean"
        },
        "muted": {
          "default": true,
          "enum": [
            true
          ],
          "type": "boolean"
        },
        "primitive": {
          "enum": [
            "video"
          ],
          "type": "string"
        },
        "rect": {
          "$ref": "#/$defs/PlaylistRect"
        },
        "selector": {
          "$ref": "#/$defs/PlaylistMediaSelector"
        }
      },
      "required": [
        "id",
        "primitive",
        "selector",
        "rect",
        "layer",
        "content_fit"
      ],
      "type": "object"
    },
    "PlaylistWrite": {
      "additionalProperties": false,
      "properties": {
        "name": {
          "maxLength": 120,
          "minLength": 1,
          "type": "string"
        },
        "pages": {
          "items": {
            "$ref": "#/$defs/PlaylistPageWrite"
          },
          "maxItems": 100,
          "minItems": 1,
          "type": "array"
        }
      },
      "required": [
        "name",
        "pages"
      ],
      "type": "object"
    },
    "PrimitiveEnter": {
      "additionalProperties": false,
      "description": "Optional object enter. Playlist write and the runtime manifest use this object as-is; there is no snake_case rename inside it. Absent means paint at rest, full opacity. No per-primitive duration or delay; those are named constants in screenrig.canvas/v1.",
      "properties": {
        "type": {
          "enum": [
            "fade-up",
            "fade-down",
            "fade-left",
            "fade-right",
            "fade-in",
            "zoom-in",
            "zoom-out"
          ],
          "type": "string"
        }
      },
      "required": [
        "type"
      ],
      "type": "object"
    }
  },
  "$id": "https://screenrig.ai/schemas/playlist-write/v1",
  "$ref": "#/$defs/PlaylistWrite",
  "$schema": "https://json-schema.org/draft/2020-12/schema"
}
