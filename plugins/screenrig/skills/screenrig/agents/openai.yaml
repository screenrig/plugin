interface:
  display_name: "ScreenRig"
  short_description: "Operate ScreenRig with the bundled CLI"
  default_prompt: "Use $screenrig to compose local stills, upload media, write playlists, and assign content to my screens with machine-readable output."
